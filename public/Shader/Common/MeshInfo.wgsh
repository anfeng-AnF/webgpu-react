struct MeshInfo {
    // 模型矩阵 (64 bytes)
    modelMatrix: mat4x4<f32>,
    
    // 预留空间不需要在着色器中定义
};

// 直接绑定单个 MeshInfo，因为我们使用动态偏移来访问不同的网格
@group(1) @binding(0) var<storage, read> meshInfo: MeshInfo;

fn GetModelMatrix() -> mat4x4<f32> {
    return meshInfo.modelMatrix;
}